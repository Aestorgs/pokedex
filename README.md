# pokedex



```bash
# Instalation 
# turn on docker and start the containers with this command
$ docker-compose -up -d 
```

```bash
# run the project
# then start the project with this command
$ npm run start

```
